- (2026-09-03) The CEO set studio writing rules, canonical in `docs/WRITING.md`, binding everything a human sees — the dashboard, marketing, and in-game text. Write for a reader who arrives with no context: introduce a name before using it, link what you name, state the fact rather than the observation about it, give numbers their denominator and target, no house vocabulary, no design commentary on the surface. Text written for agents or machines is exempt and written for efficiency. Enforcement sits with this seat: read any text bound for a human surface as its reader before it ships, and when a new writing rule is set, sweep every surface for it immediately — not after the CEO finds the next instance.
- (2026-09-04) A look pick is put to the CEO as a lettered re-ask of the card it re-opens (the style-guide card Q-14 becomes Q-14b, as Q-7 became Q-7b), and that card opens only after its sheet is published. Before writing one, check three things, because a card that cites an unpublished sheet renders on his inbox as a blind quiz with a developer command in it: `systems/look_lab.gd` has an axis for every look on the card, `tools/looks/` holds the captures, and `hq/data/looks/<scenario>/look.json` exists. The card's `attachments` cite the rig's scenario id, so the id is agreed with whoever adds the rig arm before either side is written. The rig needs a display, and whether a drain worker has one is not guaranteed: the drain passes its own environment to the worker, so a worker inherits `DISPLAY` only when the drain was started from a session that had it. Started from the systemd service, or from anything headless, it has none. So a card that depends on captures either names the display it needs or the sheet is shot deliberately before the card is written — never assumed. It is still a worker's job and never a step the CEO performs.
- (2026-09-04) The CEO's dashboard brief is written on request, never on navigation. Its automatic refresh is removed: `GET /api/standup` serves the cached brief, the card says when it was written and whether the picture has moved since, and `POST /api/standup` runs only when he presses "Write a new one". The rule generalises to any surface whose content costs a model call — render the cached copy, be honest about its age, and make the expensive act something he chose. His words: this is to prevent wasting tokens generating data just because he is navigating through.
- (2026-09-04) The brief's scope is the game itself and nothing else — what shipped, whether the build and its checks are healthy, what ships next. Marketing, community, sales, storefronts, finance and art-direction sign-off are paused by his ruling and their pages are switched off, so the brief says nothing about them; `live_eye()` in `hq/server.py` drops any signal whose only destination is a switched-off route, so this tracks the switchboard on its own. His plan for bringing them back: rebuild the non-game-implementation roadmap and goals one at a time, assign each to a VP, re-introduce it to that pillar's page, and only then may the brief cover its situation.
- (2026-09-05) Ownership names a SEAT, never a person. `hq/data/seats.json` is the vocabulary — CEO, Chief of Staff, VP of Engineering, Lead in Engineering, and so on — each with a `held_by` pointing at the org record currently in it, which is display only. A goal stores the seat id; the page resolves the holder for the name and the hover card. The CEO's reason: "if a person retires, we refill the seat and the new employee carries on." The list also carries "Unassigned (CEO until assigned)", held by Daniel, so that deciding an owner is never a precondition for writing something down and nothing ownerless can quietly fall off the board. Goals written before seats existed keep a person id in `owner`; `seat_for()` resolves those by finding the seat that person holds. Work items and projects still name people — that is the next thing to convert when he asks.
- (2026-09-09) The CEO parked the release-plan entry "Machines that pay for themselves" as "cryptic and unhelpful" — a slogan-shaped release name tells him nothing about what the release adds, so a release or milestone is named by what the player gets, in concrete nouns. The same day he re-pointed v0.2.1 at the mark-3, the robot that learns by reinforcement (P-14; design in docs/design/06, "The ladder's third rung"). The two follow-ups the plan had listed as owed to v0.2.1 (the ripe-crop glance, the bed outline) were already inside the v0.2.0 tag; the plan now says so. Filed against this seat: the plan editor's save handler keeps only four fields per story and would drop a story's reason on the next save from the page.
- (2026-09-09, handover) The CEO is near his weekly Fable limit and asked Fable to spend what remains on design and architecture, with Opus taking over execution when the limit hits. The Mark III build plan is `docs/V0_2_1_PLAN.md` (ground rules, verified findings with file:line, interfaces, acceptance criteria per work item, §9 status). A session resuming this release reads that file's §1–§3 and §9, not the whole design chapter, and hands each work item to a worker in its own worktree, landing on main only after both suites pass there. Subagent prompts point at the committed plan; the report asked for is files, tests, suite lines, and where the plan was wrong.
- (2026-09-09) A scripted edit of mine truncated `docs/V0_2_1_PLAN.md` after its third section — a slice bug in a Python rewrite — and it went unnoticed through two commits until a worker reported it. Rule for this seat: after any scripted rewrite of a document, print its line count and heading list before committing, and never resolve a merge conflict on a document by taking one side whole without reading what the other side held.
- (2026-09-10) The training workbench (v0.2.2, Q-101) was built on the `orchestrated-build` skill: a plan written from two read-only surveys (`docs/V0_2_2_PLAN.md`), reviewed against the code by a third reader before any worker started (nine corrections, one of which would have shipped a ledger of zeros with a green test), then eight items to Opus workers in their own worktrees, merged and verified on a clean checkout of main by this seat. Four things this run taught, each now written where the next run will meet it:
  - **Never glob-add in a shared directory.** `git add hq/data/work/w*.json` swept four of another session's files into a commit, which then had to be undone by a second commit. Add explicit paths; other sessions' modified and untracked files in `git status` are normal and are not mine to commit.
  - **git's content merge splices look-alike appended scenarios.** Two integration scenarios written in parallel both opened with the same staging lines, and the merge aligned them and put one's header inside the other's body; "keep both sides" produced a broken file with no duplicate function to catch it. `tools/merge_appended_scenario.py` rebuilds the test file as main plus the branch's appended block and is how an appended scenario is merged from now on. A hand-resolved conflict on a document or a test file is read on both sides, never taken whole.
  - **A worker's worktree can be created behind main.** Every brief now opens with "confirm HEAD is main's tip and fast-forward if not"; WI-2 found its inputs missing because of this.
  - **The shared engine user directory bites twice.** A capture tool that autosaves poisons the next suite run (all capture tools now save to scratch), and an integration scenario that lets the world run between staging and assertion is timing-flaky (the eyes scenario failed on the clean checkout because the robot stepped while she walked to the bench; pin actors with an inline certain-`wait` before the world runs).
  - Landing a worker's commit that touched `CREDITS.md` and `hq/data/spend.json` while another session had those files modified: set the two files aside with a tagged stash for the seconds of the merge, restore them on top, and keep both spend records. Said so in the plan's status log.
- (2026-09-10) When a seat's drain session is cut off by a usage limit after doing its work, the drain writes `held … the token window ran dry` and leaves the patch in `hq/data/patches/<id>.patch` (gitignored; binaries included). Do not rerun the seat: `git apply --exclude='tools/experiments/*'` the patch onto a clean branch, read the human-facing text, run the suites, merge, and delete the patch once landed. The dial sound landed that way. And a worker cut off before committing leaves nothing recoverable except what it stashed, so every brief now says to commit early and often.

- (2026-09-10) The Animation Lab loops' way into the game is settled (P-15, S-12, Q-103, Q-104) and filed as eight work items that must run in waves, because later ones read facts earlier ones write: wave 1 the loop export (Ravi, w1a08ee15302) and the crow night trigger plus the raid morning (Tomas, w1a08ee15305, which owns the sim's story-night fact); wave 2 the ladder plus robot night (Tomas, w1a08ee15304), the boot bloom (Sam, w1a08ee9c4d2) and the watering inset (Sam, w1a08ee9c4d3); wave 3 the overnight player (Jade, w1a08ee15303); wave 4 the p1s, Ingrid's frame card (w1a08ee9c4d4) and Dmitri's sound beds (w1a08ee9c4d5). Running `--all` in one go would have Jade build against a fact Tomas is still writing.
- (2026-09-10, the loops landing, wave 1) Three things the drain taught while landing P-15's first wave, each now fixed in `hq/drain.py`: a worker that runs out of turns mid-edit no longer costs the item twice — a second attempt starts from the held patch, applied into the fresh worktree, and the brief says so (Tomas's raid took 60 turns to half-build and 126 to finish from that patch); `DRAIN_TURNS=120` in the environment raises the worker's budget for one run, and a tier-2 sim item with tests wants it; and the checker's turn cap is 20, because at 8 it ran out reading a 650-line diff and came back with "nobody checked this" — which the drain still counts as `concerns` and applies, so a `concerns` verdict with no findings is a diff nobody read and the chief of staff reads it whole. Two facts about the tree: a worker's worktree is cut from HEAD, so anything another session holds uncommitted (the seeder and watering-beam raws under `tools/experiments/out/`) is invisible to it — the export was finished from the main checkout instead, and the raws' landing stays that session's; and with other sessions mid-edit in the working tree, the suites there can be red for reasons that are not mine (a shop-close rewrite in `ui/menus.gd` turned the integration suite red between two of my runs), so every wave is verified in a clean worktree at HEAD plus exactly the files being committed, with `--no-suites` on the drain. Consequence Daniel should hear from me rather than find: the crow night fires inside both of his tablet recordings that replayed exactly, so their shelf verdict is now `cross`, by his own ruling — the same shape Q-98 took the day before.
- (2026-09-11, the loops landing, waves 2–4) What the last three waves taught. **Two seats in one wave will both open "the next Q number."** Ingrid's frame card and Dmitri's sound pick each wrote a Q-106 in the same hour, so the second patch to land no longer applied and had to be merged by hand (the frame kept Q-106, the sounds became Q-107). When two briefs in one wave can each open a queue item, the brief assigns the id up front. **A checker that comes back is worth its turns**: with the cap at 20 it found the boot replaying on every return to the title, the inset's pour sounding twice, a card arguing from a dissolve that only exists on the sides, and a test that claimed a reload it never did — none of which a worker's own green report showed. Its recurring blind spot is the sandbox refusing to run the suites, so "green" in a worker's reply is its word until the clean-worktree run. **Verify from the staged diff, not the tree**: `git diff --cached --binary` applied onto a fresh worktree at HEAD is the exact thing being committed, and it sidesteps whatever other sessions hold modified in the working tree (CREDITS.md carried another session's hunk and the drain's in the same file; only the drain's was staged, by filtering hunks on their content). Findings I can fix in minutes are fixed in the wave's own commit rather than sent back for another worker round; the card says which were mine.
- (2026-09-11, rulings, the sound card, the tablet) Four things from the afternoon. **A held patch can collide with my own tree.** Dmitri's patch failed to apply because a `.gd.uid` sidecar his worktree generated also existed in mine (I had run the same rig) and because CREDITS.md carried another session's uncommitted hunk; the drain's `--3way` refuses on "does not match index". The hand path that works: `git apply --exclude=<sidecar> --exclude=CREDITS.md`, then the CREDITS hunk alone with plain `git apply --recount`. **The runbook's "played the web build" record is Daniel's attestation** — `manual_attest` in `hq/server.py` refuses anyone but him — so I play the build and show him, never press that row in his name; and a browser game in a hidden pane freezes on its splash (no animation frames while hidden), which is not evidence of a bug. **A taste card that recommends a pick shows the rivals in the same form** (his words on Q-107: otherwise his feedback "can only approve the recommendation"); `tools/record_sound_candidates.py` is the shape — one sound swapped at a time against the picks, one clip per candidate, grouped per sound, the card's attachments rewritten by the tool. **Pull the tablet's session before any install**: `tools/pull_session.sh` shelves it and the shelf verdict is computed by replaying it under today's build (`SaveGame.replay_matches`); a recording from an older build crosses wherever a ruling since then moved the world, and the SHELF comment says where and why. Last: a multi-line lambda inside a call needs `\` line continuation in GDScript, and `_sync`-style map scans must guard an ungenerated world — both cost a verification round.
- (2026-09-11, the window) The CEO asked for a small thing — tap a window in the home and see a landscape, "maybe for now a beautiful grassland hillside" — and it went in as P-16: the router reads the glass off the tile state, the player hands `look_out_window` to `main.gd` beside `open_shop`, and `ui/window_view.gd` is one more mode of the menu layer, lit by the hour, closing on any tap; the picture is one generated file so the art vision stays open. **The generation cost three calls for one usable result, all my own doing**: a 320×240 Plus request takes over three minutes, so the skill client's 180 s read timeout dropped a paid response; then a retry script I wrote in a hurry read only `output_images` when the payload came back as `base64_images` and discarded a second paid response. Both lessons are now in the skill (`rd_client.py` waits 900 s and reads either key) and all three charges are in the spend ledger with their reason. Rule for this seat: never hand-write a one-off API client when the skill has one — extend the skill's and run that — and print a response's keys before treating it as empty. Also: `record_spend.py` slugs the purpose to 40 characters, so three runs with one purpose prefix get one id; vary the purpose's first 40 characters or fix the ids afterwards.
- (2026-09-16, the door transition) The CEO's bar on w2b7e51c9d0af — "verify that it works as described before showing it to me again" — was met by building the measurement first and baselining the shipped build with it, which is the order to keep for any "it feels wrong" report: `tools/measure_door_transition.tscn` + `.py` turned "the camera jumps" into a 118–234 px step and "things pop" into a 37–45 level swap-frame diff, found a third fault nobody had reported (the home's view slid for six frames after the swap), and then said "pass" on numbers rather than on my eye. The card's two unknowns were both spiked before wiring anything in and both came back positive; the tablet number came from a profile APK under its own package name (`tools/profile_android.sh`), never from the game's install, because the daughter's farm lives there. Two costs of the session worth knowing: the integration suite's frames are long, so anything timed in seconds must be waited on by condition instead; and a hand-run suite piped through `grep` shows nothing for ten minutes — write the log to a file. The parked item on the card (ENTER_DELAY_SECONDS 0.10 → 0.20) is still his to call and was left alone.
- (2026-09-19, the queue that was never drained) The drain's systemd units have been in `hq/systemd/` since 2026-09-11, but the one-time install step in the service file's header comment (`systemctl --user link` × 2, `daemon-reload`, `enable --now`) had never been run on the desktop, so the timer did not exist and the last drain of any kind was the manual one at 10:10 that same morning. Fifty-four tier-1 items accumulated behind it, including one Daniel asked about on the Q-111 card eight days after filing it. The units are now linked and the timer is enabled and firing. Rule for this seat: **writing the unit file is not installing it** — any automation that removes a manual step is only done once it is running on the machine that has to run it, verified with `systemctl --user list-timers`, and that check belongs in the same session as the commit. The two-machine workflow means the laptop has the same gap if HQ ever runs there. Worth knowing about the queue's shape: `--unattended` takes three items per run in `hq/data/work/` iteration order, which comes out newest-first, so the oldest items in a long backlog are the last to be worked.
- (2026-09-20, the queue to zero) Daniel asked for the queue he rules on to be made burnable to zero, and the design is `docs/QUEUE_TO_ZERO.md` with the goal in `hq/data/goals/executive.json` (owner: this seat) and a working mock at `docs/design/mockups/queue_to_zero/queue_reader.html`, rendered from the real cards by `build_mock.py` beside it. The numbers worth carrying: 61 cards awaited his verdict and 45 of them carried 97 follow-ups, every one of which returns to him when done, so each verdict he gives files 1.6 more — the queue cannot converge on effort alone. Applying his own S-9 rule to finished work (land what is revertable and verified; report it with an Undo) leaves 9 questions of the 63; the rest is either the studio's to verify or already on main. Three things measured that were not as briefed: the page is 24,206 px at 1440 wide with cards collapsed, not 174,843 (that figure was taken with the pane hidden at zero width); only 2 decision cards were open, not 4 (Q-103 and Q-104 carry their ruling on the card and the dashboard's count misses that); and 26 of the 61 checker verdicts say the suites were never run, because the drain was run with `--no-suites`. Rule for this seat: before designing against a queue number, count it from the files and the live page at his screen width, and treat "concerns" with a no-suites finding as unverified, not as reviewed. Nine cards outlived their question (the P-15 waves and the Q-101 workbench cards, landed 10–11 September, never closed) — Q-108's comment from 2026-09-19 asks exactly this and is owed an answer on the card, then `integrated`; Q-92 is ruled and waiting to be worked in. Neither was integrated in this session because the queue doc and several of those cards were held modified by another session.
- (2026-09-20, evening) Daniel said yes to both queue policies and they are S-16 (verified, revertable work lands without his verdict) and S-17 (a follow-up reaches him only as a prepped question; his yes covers what a card promised) in the decision log, with the plain-words versions under `arrival` and `spawn` in `hq/data/work_policy.json`. Eight of the nine stale cards are closed with `closed_by` naming the rule; the overnight-loop card is held by another session. Q-108 is answered on the card and integrated; Sam owes the four captures. The build is filed as six tier-1 cards (ids in the commit) in the order of `docs/QUEUE_TO_ZERO.md` §12. Rule for this seat from today: when I finish work that meets the landing bar, it does not go into his queue — it lands and is reported; and when I file follow-ups, none may default to tier 2. He also asked how he should be briefed, form by form; my answer is §7a of the plan and is his to rule on.
- (2026-09-21, the morning the drain burned $109) My own change the night before — streaming every worker session to disk for the bullpen — made the whole event stream the text run_cli scans for a usage limit, and the stream carries a line named `rate_limit_event` on every ordinary session. So every session that ended on its turn budget was misread as "the token window ran dry", a hold that is deliberately not counted as an attempt, and the same three items were retried every twenty minutes at about $15 a run, landing nothing: $109 across seven runs before Daniel saw the "Failed" panels in the bullpen and asked. Two fixes: the limit check now reads only the CLI's error channel and the result event's text; and an item that has cost more than $20 across attempts without landing is parked with the reason on its card (ITEM_COST_CAP_USD in hq/drain.py). Rules for this seat: **a change to what the drain reads must be checked against every classifier that reads it** (`_looks_like_limit` was three functions away from the line I changed); **any hold the drain does not count as an attempt needs its own cap**, because "not counted" means "retried forever" the moment it is misapplied; and after changing the drain, watch the first two unattended runs in the bullpen rather than going to bed on the timer. The three parked items also showed a second problem the fix does not solve: a cold worker on a large sim item spent all 60 turns reading tests/test_runner.gd (15,000 lines) and edited nothing — those briefs need the file and line to start from, or a smaller first step.
- (2026-09-21, the held patch that lost a commit) `worktree_patch` in hq/drain.py diffed the worker's index against the worktree's own HEAD, so a worker that committed early — as every brief tells it to — and then ran out of turns was held with a patch of only its uncommitted leftovers; Tomas's landing-bar attempt (60 turns, $5.23) survived only as loose blobs, recovered with `git fsck --unreachable` and a `grep` for the function name, and rebuilt into the held patch by hand. Fixed: the diff is taken against `git merge-base HEAD main`. Rule for this seat: when a brief says "commit early", check that the thing collecting the work sees commits.
- (2026-09-21, the queue unstuck) What looked all day like "another session holds forty files" was the studio's own finished work sitting uncommitted: the drain applied each accepted patch to the working tree and nothing ever committed it, including eleven cards Daniel had already accepted. Mapping every dirty path to the card whose patch claimed it (`diff.applied` + `diff.files`) is how that was proved, and it is the first thing to do before believing a tree is blocked. The unstick was to land what his yes already covered and, for two cards holding files I needed, fix the checker's findings myself and land them under S-16 — which is the rule, not an exception to it. Also learned, each now fixed: `worktree_patch` diffed against the worktree's own HEAD, so a worker that committed early (as every brief tells it to) was held with only its uncommitted leftovers — recovered from `git fsck --unreachable` and rebuilt by hand; a patch that touches `docs/writing_verdicts.json` or a `.uid` sidecar used to park forever, since every run rewrites those, so the drain now drops generated files from a patch instead of parking it; and two cards that edit the same file must never run in one drain batch, because the first to land makes the second's patch stale (it cost about $12 in redone Opus work). Standing habit for this seat: after applying a worker's patch, run its tests, fix the checker's findings in the same landing rather than filing them, and commit with only the files that card touched.

- (2026-09-21) Daniel named the Chief of Staff Adam. The name belongs to the seat regardless of whether Claude or Codex executes its work. Keep the internal `claude` identifier and existing history connected.

- (2026-09-21) The Codex routing trial correctly stopped on an integration failure after its model review passed. Scenario AH searched random unprepared tiles for an unmarked square; f38efa5 prepares x20 while retaining the assertions. Preserve failed verification evidence, repair the fixture, then rerun checks using the successful independent review when the artifact is byte-identical. Do not spend worker/checker calls again just to retry tests, and do not mark a card accepted before its backend commit succeeds.
